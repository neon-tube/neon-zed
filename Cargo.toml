[package]
name = "zed-neon"
version = "0.1.0"
edition = "2021"
publish = false

# Zed compiles extensions to wasm32-wasip2 and loads them as a component.
[lib]
crate-type = ["cdylib"]

[dependencies]
zed_extension_api = "0.7"
